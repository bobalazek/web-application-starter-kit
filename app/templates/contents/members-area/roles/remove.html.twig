{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Remove' | trans }} | {{ 'Roles' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {% if roles %}
            {{ 'Remove Roles' | trans }}
        {% else %}
            {{ 'Remove Role' | trans }}
            <small>"{{ role }}"</small>
        {% endif %}
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.roles') }}">
                {{ 'Roles' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Remove' | trans }}
        </li>
    </ol>
    <div class="alert alert-block alert-warning">
        <h4 class="alert-heading">
            {{ 'Warning!' | trans }}
        </h4>
        {% if roles %}
            <p>{{ 'Are you sure you want to remove this roles?' | trans }}</p>
            <ul style="margin-bottom: 10px;">
                {% for role in roles %}
                    <li>{{ role }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ 'Are you sure you want to remove this role?' | trans }}</p>
        {% endif %}
        <p>
            {% if roles %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.roles.remove', { id: 0, action: 'confirm', ids: ids }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% else %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.roles.remove', { id: role.getId(), action: 'confirm' }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% endif %}
            <a class="btn btn-success"
                href="{{ url('members-area.roles') }}">
                {{ 'Cancel' | trans }}
            </a>
        </p>
    </div>
{% endblock %}
